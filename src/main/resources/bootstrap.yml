spring:
  application:
    name: PORTAL-NEWS-SIDECAR
  cloud:
    nacos:
      server-addr: 127.0.0.1:8848
server:
  port: 8087
management:
  info:
    defaults:
      enabled: true
  endpoint:
    health:
      show-details: ALWAYS
  endpoints:
    web:
      exposure:
        include: '*'
